fastapi>=0.110.0
uvicorn>=0.28.0
pydantic>=2.6.0
pydantic-settings>=2.2.0
python-dotenv>=1.0.1
httpx>=0.27.0
websockets>=12.0
openai>=1.14.0
chromadb>=0.4.24
tiktoken>=0.6.0
numpy>=1.26.0
python-multipart>=0.0.9
jinja2>=3.1.3
slowapi>=0.1.9
vieneu>=3.0.12


# ── Voice / Interview Dependencies ─────────────────────────────
# Google Cloud STT (async) — primary STT provider, free 60min/mo
google-cloud-speech>=2.30.0

# Google Cloud TTS (async) — fallback TTS provider, free 1M chars/mo
google-cloud-texttospeech>=2.18.0

# Edge TTS — primary TTS (free, Microsoft neural voices, excellent Vietnamese)
edge-tts>=6.1.10

# Redis — session state, draft storage, TTS cache, conversation history
redis[hiredis]>=5.0.0

# PyAV — universal audio decoder (webm/opus → WAV PCM)
# This is the CENTRAL piece that fixes all format issues:
# - Makes silence detection meaningful (RMS on decoded PCM)
# - Makes Faster-Whisper work without system ffmpeg
# - Makes Google STT use LINEAR16 (no WEBM_OPUS issues)
av>=12.0.0

# Faster-Whisper — cheap local/testing STT (CPU/int8)
# Production can set STT_PRIMARY_PROVIDER=google if memory is constrained.
faster-whisper>=1.0.0

# RapidFuzz — local deterministic typo correction for technical terms
rapidfuzz>=3.9.0
litellm>=1.34.0
tenacity>=8.2.3
tqdm>=4.66.2
psutil>=5.9.0

